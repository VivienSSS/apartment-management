package web

templ DashboardPageMetadata() {
	<title>Login Page</title>
}

templ DashboardPage() {
	@BaseHTML(BaseHTMLProps{
		Metadata: DashboardPageMetadata(),
	}) {
		<main class="h-screen flex">
			<body class="bg-slate-200 flex">
				<div class="flex flex-col bg-slate-600 p-4 w-64 h-screen"></div>
			</body>
		</main>
	}
}
